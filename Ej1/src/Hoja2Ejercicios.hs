module Hoja2Ejercicios 
            (componer,
            impar,
            signum',
            bisiesto,
            sumaCuatupla) where

componer :: Int -> Int -> Int
componer x y = max x y

impar :: Int -> Bool
impar x 

signum' :: Int -> Int
signum'

bisiesto :: Int -> Bool
bisiesto

sumaCuatupla :: (Int, Int, Int, Int) -> Int
sumaCuatupla (x, y, z, w) = x + y + z + w

